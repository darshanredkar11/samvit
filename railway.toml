[build]
dockerfile = "Dockerfile"
dockerfileTarget = "runtime"

[deploy]
startCommand = "samvit serve --host 0.0.0.0 --port 8765"
healthcheckPath = "/ready"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
